# Macula Infrastructure Environment Variables
# Copy this file to .env and customize for your environment

# PostgreSQL password for PowerDNS backend
POSTGRES_PASSWORD=powerdns-dev-secret

# PowerDNS HTTP API key (used by ExternalDNS)
POWERDNS_API_KEY=macula-dev-api-key

# PowerDNS Admin secret key
PDNS_ADMIN_SECRET_KEY=macula-admin-dev-secret

# Data directory for volume mounts (optional, defaults to ./data)
# DATA_DIR=/path/to/data
