apiVersion: v1
kind: Service
metadata:
  name: macula-console
  namespace: macula
spec:
  type: NodePort
  selector:
    app: macula-console
  ports:
  - name: http
    port: 80
    targetPort: 4000
    nodePort: 30600
